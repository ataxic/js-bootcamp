let myPerson = {
    name: "Joffrey Verkade",
    age: 43,
    location: "Sheffield, United Kingdom"
}

console.log(`${myPerson.name} is ${myPerson.age} years old and is from ${myPerson.location}.`);

myPerson.age = myPerson.age + 1;

console.log(`${myPerson.name} is ${myPerson.age} years old and is from ${myPerson.location}.`);
